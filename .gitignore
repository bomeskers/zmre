# In this file we determine which files we don't want to upload to Github.
# These files can either contain sensitive information that should not be made public
# or files that are part of a package manager, like node_modules.

# node_modules are files generated when doing yarn install, and should not be committed.
node_modules

# The dist folder contains files generated by the yarn start or yarn build process.
# They are results of work in the src folder and should not be committed.
dist

# These guys are files generated on OSX systems and have nothing to do with the project.
.DS_Store
